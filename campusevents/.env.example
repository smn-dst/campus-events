# ═══════════════════════════════════════════════
# CampusEvents — Variables d'environnement
# ═══════════════════════════════════════════════
# Ce fichier contient des valeurs par défaut fonctionnelles
# pour le développement local.
#
# Pour la production, remplacer les secrets !
#   cp .env.example .env
#   Puis modifier JWT_SECRET et POSTGRES_PASSWORD
# ═══════════════════════════════════════════════

# ── PostgreSQL ────────────────────────────────
POSTGRES_USER=campus
POSTGRES_PASSWORD=secret123
POSTGRES_DB=campusevents

# ── Connexion DB (utilisée par Prisma) ────────
DATABASE_URL=postgresql://campus:secret123@campus-db:5432/campusevents

# ── Redis ─────────────────────────────────────
REDIS_URL=redis://campus-redis:6379

# ── JWT ───────────────────────────────────────
JWT_SECRET=dev-campus-jwt-secret-change-in-prod
JWT_EXPIRES_IN=7d

# ── Backend ───────────────────────────────────
NODE_ENV=production
PORT=3000

# ── Frontend (build time) ─────────────────────
VITE_API_URL=http://api.localhost
