# ═══════════════════════════════════════════════
# CampusEvents — Variables d'environnement
# Copier ce fichier : cp .env.example .env
# Puis remplir les valeurs secrètes
# ═══════════════════════════════════════════════

# ── PostgreSQL ────────────────────────────────
POSTGRES_USER=campus
POSTGRES_PASSWORD=CHANGE_ME_secret123
POSTGRES_DB=campusevents

# ── Connexion DB (utilisée par Prisma) ────────
DATABASE_URL=postgresql://campus:CHANGE_ME_secret123@campus-db:5432/campusevents

# ── Redis ─────────────────────────────────────
REDIS_URL=redis://campus-redis:6379

# ── JWT ───────────────────────────────────────
JWT_SECRET=CHANGE_ME_generate_a_random_string
JWT_EXPIRES_IN=7d

# ── Backend ───────────────────────────────────
NODE_ENV=production
PORT=3000

# ── Frontend (build time) ─────────────────────
VITE_API_URL=http://api.localhost